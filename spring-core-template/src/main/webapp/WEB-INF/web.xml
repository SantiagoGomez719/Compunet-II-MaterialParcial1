<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <!-- 1. Dónde está la configuración de Spring.
         VERSION XML (activa por defecto): -->
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>classpath:applicationContext.xml</param-value>
    </context-param>

    <!-- VERSION JavaConfig: si usas la clase @Configuration en vez de XML,
         cambia el listener por AnnotationConfigWebApplicationContext y el
         param-value por el nombre completo de la clase @Configuration:

    <context-param>
        <param-name>contextClass</param-name>
        <param-value>org.springframework.web.context.support.AnnotationConfigWebApplicationContext</param-value>
    </context-param>
    <context-param>
        <param-name>contextConfigLocation</param-name>
        <param-value>com.compunet.config.AppConfig</param-value>
    </context-param>
    -->

    <!-- 2. Este listener es de Spring mismo (spring-web). Arranca el
         WebApplicationContext UNA VEZ cuando Tomcat levanta la app y lo
         deja disponible para todos los Servlets. -->
    <listener>
        <listener-class>org.springframework.web.context.ContextLoaderListener</listener-class>
    </listener>

</web-app>
